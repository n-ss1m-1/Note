# 个人信息
陈业伟  
15278400113
chenyw233@mail2.sysu.edu.cn


# 教育背景
中山大学(985)
软件工程-本科
2024.9 - 2028.6
- 英语：CET-6


# 专业技能

- 熟悉C/C++基础语法和面向对象编程，熟悉STL常用容器与C++11核心新特性(智能指针、右值引用等)
- 熟悉常见数据结构与算法，包括链表、二叉树、哈希表，以及快速排序、归并排序等
- 熟悉HTTP/HTTPS、TCP、UDP等常见协议，熟悉TCP三次握手、四次挥手、流量控制、拥塞控制等机制
- 熟悉操作系统的基本概念，包括内存管理、进程/线程管理、死锁、文件系统、零拷贝技术
- 熟悉Linux下的TCP/UDP Socket编程、 I/O多路复用技术与Reactor事件驱动模型
- 熟悉MySQL的使用，了解事务、索引、锁等原理，对InnoDB存储引擎及MVCC机制有一定了解
- 了解Git和GitHub的基础操作，能通过CMake构建简单的C/C++项目。

# 项目经历

我接下来要写我的项目经历 第一个项目技术栈：Linux C++ epoll I/O多路复用 Reactor模式 线程池 异步日志系统 Cmake 智能指针 function和bind包装回调函数 零拷贝sendfile
通过 muduo 主从 Reactor + 线程池的架构，充分利用多核 CPU，在高并发场景下将服务吞吐量提升 **110%**，延迟降低 **22%**，最终实现 **6.2 万 QPS** 的高性能 HTTP 服务。


**项目名称**：C++ 高并发网络服务器
**项目简介**：基于 **Linux C++** 自主设计高性能事件驱动服务框架，采用 **主从 Reactor + 线程池** 架构，**Epoll 边缘触发（ET）** 与非阻塞 I/O 实现高并发 TCP/HTTP 服务。
1. **主从 Reactor + 线程池架构**：主 Reactor 监听连接，子 Reactor 分发 I/O 任务至线程池，**充分利用多核 CPU**，实现连接接入与计算解耦。
2. **Epoll ET 模式非阻塞 I/O优化**：通过边缘触发机制与事件回调，**高效分发就绪事件**，避免线程阻塞，提升 I/O 吞吐能力。
3. **双缓冲异步日志系统**：日志前端写入内存缓冲区，后端线程落盘并支持滚动，**避免同步 I/O 阻塞网络线程**。
4. **智能指针内存管理**：通过 `shared_ptr`/`weak_ptr` 安全管理连接生命周期，**杜绝悬空指针与内存泄漏**。
5. **零拷贝文件传输**：静态资源请求通过 `sendfile` 优化，**避免用户态/内核态数据拷贝**。
6. **成效**：**QPS 达 62,000+**（较单线程提升 **110%**），**5000+ 并发连接稳定运行**，服务延迟降低 **22%**。
---

**非阻塞IO**：
1. **设置位置**：
    
    - Socket 创建时（`SOCK_NONBLOCK`）
        
    - accept 接收连接时（`accept4`+ `SOCK_NONBLOCK`）
        
    - epoll 注册时（`EPOLLET`边缘触发）
        
    
2. **体现位置**：
    
    - `sendInLoop`中的非阻塞 write 和 EAGAIN 处理
        
    - `handleRead`中的非阻塞 read 和循环读取
        
    - `Buffer::readFd`中的 readv 和栈缓冲区优化
        
    - 整个事件循环的无阻塞设计





## 项目二
### **项目名称**：基于 Qt/C++ 的航空订票系统
**项目简介**：基于 **Qt 网络框架** 与 **MySQL** 构建高可靠服务端，采用 **自定义 JSON 协议** 实现客户端-服务端全双工通信，支撑全流程业务（查询、下单、支付、改签），通过事务与连接管理保障数据一致性。

1. **设计数据库接口层**：基于单例模式封装连接池、事务控制及强类型API，使数据库操作代码量减少 **40%**，提升可维护性与安全性。
2. **实现多维度动态航班查询**：通过`FlightQueryCondition`结构体动态拼接SQL条件，自动追加余座>0约束，确保查询准确率 **100%**，接口响应时间稳定 **<200ms**。
3. **保障事务一致性**：在订单改签业务中封装多步原子操作（取消原单→创建新单），通过数据库事务（`BEGIN`/`COMMIT`）确保航班余座、订单状态联动更新，**1000次测试中，并发数据一致率 100%**。
4. **开发高效请求处理器**：基于`QTcpSocket`解决TCP粘包问题，通过JSON`type`字段路由业务请求，支持 **200+ 并发连接** 与 **20+ 业务类型**，代码结构清晰易扩展。




在客户端发送数据时，**强制在每条 JSON 消息末尾添加 `\n` 分隔符**（例如：`{"type":"query","city":"BJ"}\n`）。  
服务端使用 `QTcpSocket` 的 `readyRead()` 信号监听数据，**按 `\n` 分割缓冲区**，确保每次解析到一个完整消息。

**技术细节**（面试官会问）：
```
1// 服务端核心代码片段（ClientHandler.cpp）
2void ClientHandler::onReadyRead() {
3  QByteArray data = socket->readAll();
4  buffer.append(data);  // 累积数据到缓冲区
5
6  while (buffer.contains('\n')) {
7    int pos = buffer.indexOf('\n');
8    QString msg = buffer.left(pos);  // 提取完整消息
9    buffer.remove(0, pos + 1);       // 移除已处理部分
10
11    // 解析 msg 为 JSON 并路由到对应业务
12    handleRequest(msg);
13  }
14}
```

**为什么有效**：

- TCP 本身无消息边界，但通过**约定分隔符**（`\n`）在应用层强制划分消息，避免粘包。
- 无需额外协议（如长度前缀），符合您项目中“自定义 JSON 协议”的设计（简单高效，学生项目真实场景）。

> ✅ **面试验证**：  
> 面试官问：“为什么不用长度前缀？” → 回答：“为简化协议设计（JSON 本身是文本），在课程项目中优先保证可维护性，实际测试中未出现粘包问题。



### 版本一：校招简历精简版（适配简历排版，突出核心亮点）

# 基于 Qt 的航空订票系统

项目周期：XXXX.XX - XXXX.XX | 4 人团队 | 技术栈：C++、Qt 6、MySQL、ODBC、TCP/IP、JSON

项目描述：本项目为 C/S 架构的桌面端航空订票系统，完整模拟民航订票全业务流程。采用 Qt Widgets 开发客户端交互界面，Qt Network 实现服务端网络通信与业务逻辑处理，基于 TCP 自定义 JSON 协议完成前后端数据交互，MySQL 实现数据持久化，支持用户注册登录、航班多条件查询、订单创建 / 支付 / 改签 / 取消等核心功能。

**个人职责**

- 主导系统 MySQL 数据库设计，完成用户、航班、订单、常用乘机人 4 张核心业务表的结构设计与搭建，完成 ODBC 数据源配置与 Qt SQL 驱动适配。
- 基于单例模式设计并实现 DBManager 数据库访问层，统一封装数据库连接管理、事务控制、通用增删改查接口，以及全业务场景的 SQL 操作与 ORM 映射。
- 负责服务端 ClientHandler 核心模块开发，完成 TCP 粘包 / 拆包处理、JSON 协议解析、请求分发、权限校验、响应封装全流程，支撑客户端全业务请求处理。
- 设计实现在线用户管理模块，基于 QMap 建立 TCP 连接与用户信息的映射关系，完成用户登录态全生命周期管理与异常断连资源清理。
- 独立完成用户认证、航班动态查询、订单全生命周期管理等服务端核心业务开发，攻克动态条件 SQL 拼接、订单事务一致性两大技术难点。

**核心成果**

1. 封装的 DBManager 模块实现数据库操作全场景覆盖，代码复用率提升 80%，通过参数绑定规避 SQL 注入风险，单例模式保障连接全局一致性，零资源泄漏问题。
2. 实现多维度动态航班查询，支持出发 / 到达城市、日期 / 时间、价格区间等自由组合条件检索，查询响应耗时 < 100ms，仅返回有余座航班，覆盖全场景业务需求。
3. 基于 MySQL 事务实现订单改签原子性操作，通过事务复用完成原订单取消、新订单创建、余座更新、差价计算全流程一致性保障，零数据不一致故障。
4. 系统完成全业务闭环测试，覆盖 100% 核心功能与边界异常场景，支持多客户端并发访问，服务端稳定运行无崩溃，顺利完成项目验收。

---

### 版本二：面试深挖详细版（适配面试讲解、项目报告，还原技术细节）

# 基于 Qt 的航空订票系统项目经历

## 一、项目基本信息

- 项目名称：基于 Qt 的航空订票系统
- 项目周期：XXXX.XX - XXXX.XX
- 团队规模：4 人团队
- 技术栈：C++11、Qt 6、MySQL 8.0、ODBC 驱动、QTcpSocket、JSON 自定义通信协议
- 项目架构：采用客户端 + 服务端的 C/S 分层架构，客户端负责界面交互与请求发送，服务端负责业务逻辑处理与数据持久化，前后端通过 TCP 长连接通信，采用换行符分隔的 JSON 格式自定义协议，实现界面逻辑与业务逻辑完全解耦。
- 项目目标：完整模拟真实民航订票全业务流程，实现用户注册登录、个人信息与常用乘机人管理、航班多条件查询、订单创建 / 支付 / 取消 / 改签等核心功能，保障数据一致性与系统并发稳定性。

## 二、个人核心职责

作为项目服务端核心开发成员，主导数据层与核心业务逻辑开发，具体负责：

1. 系统 MySQL 数据库全量设计，包括核心业务表的字段定义、主键外键约束、索引设计，以及 ODBC 数据源配置与 Qt SQL 驱动的适配调试。
2. 基于单例模式设计并封装 DBManager 数据库访问层，统一实现数据库连接管理、事务控制、通用增删改查接口，以及全业务场景的 SQL 操作封装。
3. 服务端 ClientHandler 核心模块开发，完成 TCP 粘包 / 拆包处理、JSON 协议解析、请求分发、登录态校验、业务逻辑调用、响应数据封装全流程。
4. 在线用户管理模块设计与实现，基于 QMap 建立 TCP 连接实例与用户信息的映射关系，实现用户登录态管理、上下线状态同步、异常断连资源清理。
5. 服务端核心业务逻辑开发，攻克动态条件查询、订单事务一致性等核心技术难点，完成服务端与数据库、客户端的集成联调与测试。

## 三、核心开发工作与技术实现

### 1. 数据库设计与数据访问层封装

- **核心业务表设计**：设计并搭建 4 张核心表，覆盖全业务流程：
    
    - 用户表 (user)：存储用户账号、密码、实名信息、手机号等，设置用户名为唯一索引，保障账号唯一性。
    - 航班表 (flight)：存储航班号、起降城市、起降时间、价格、座位数等，设置起降时间联合索引，优化查询性能。
    - 订单表 (orders)：存储订单关联用户、航班、乘机人信息、订单状态、金额等，设置用户 ID、航班 ID 外键约束，保障数据完整性。
    - 常用乘机人表 (passenger)：存储用户关联的乘机人信息，设置用户 ID + 姓名 + 身份证号联合唯一索引，避免重复添加。
    
- **DBManager 单例封装**：
    
    - 采用懒汉单例模式，禁用拷贝构造与赋值运算符重载，保障全局唯一的数据库连接实例，避免多模块重复创建连接导致的资源浪费与状态不一致。
    - 统一封装数据库连接 / 断开、状态检测接口，兼容多版本 MySQL ODBC 驱动，提升环境适配性。
    - 封装通用 Query 与 update 接口，采用参数绑定执行 SQL，彻底规避 SQL 注入风险，同时统一错误处理与日志输出，简化业务层调用。
    - 封装事务控制接口（begin/commit/rollback），为订单创建、取消、改签等关键业务提供原子性操作保障。
    - 针对 4 张核心表，封装全场景增删改查接口，实现查询结果集与 C++ 结构体的 ORM 映射，屏蔽原生 SQL 细节，降低层间耦合。
    

### 2. 服务端请求处理核心模块开发

- **TCP 粘包 / 拆包处理**：在 ClientHandler 中实现基于换行符的拆包逻辑，通过缓冲区累积 TCP 数据，循环查找换行符分隔完整 JSON 消息，解决 TCP 流式传输的粘包问题；同时对非法 JSON 做容错处理，避免单条错误消息导致服务端崩溃。
- **请求分发与权限校验**：统一解析客户端 JSON 请求，提取协议 type 与业务参数，按 type 分发到对应业务分支；对所有需登录的接口，先通过在线用户模块校验登录态，拒绝未登录非法请求，保障接口安全。
- **全业务请求处理**：实现登录 / 注册、信息修改、常用乘机人管理、航班查询、订单全生命周期管理等全业务分支，完成参数校验、数据库调用、业务规则校验、响应封装全流程，支撑客户端所有业务请求。

### 3. 在线用户管理模块实现

基于`QMap<ClientHandler*, Common::UserInfo>`建立 TCP 连接实例与用户信息的映射关系，实现用户登录时的信息绑定、退出 / 断连时的信息清理，全局统一管理在线用户；提供登录态校验、用户信息更新接口，为请求权限校验提供支撑，同时实现异常断连时的自动资源清理，避免内存泄漏与无效连接占用。

### 4. 核心技术难点攻克

#### 难点 1：多维度动态条件航班查询实现

- **问题**：用户可自由组合出发 / 到达城市、日期范围、时间范围、价格区间等查询条件，部分条件可为空，固定 SQL 无法满足灵活查询需求，同时需保障 SQL 拼接的安全性与查询性能。
- **解决方案**：
    
    1. 设计`FlightQueryCondition`结构体统一封装所有查询条件。
    2. 采用动态 SQL 拼接逻辑，通过`QStringList`存储 where 条件子句，`QList<QVariant>`存储绑定参数，仅当条件有效时才加入子句与参数列表，避免无效条件干扰查询结果。
    3. 强制追加`seat_left>0`条件，仅返回有余座的航班，贴合业务场景。
    4. 所有查询值均采用参数绑定传入，而非字符串拼接，彻底规避 SQL 注入风险。
    5. 最终 SQL 按起飞时间升序排列，符合用户查询习惯。
    
- **效果**：实现全维度自由组合的航班查询功能，SQL 逻辑健壮无漏洞，查询响应耗时 < 100ms，覆盖用户所有检索场景，同时保障了查询的安全性与性能。

#### 难点 2：订单改签业务的事务一致性保障

- **问题**：改签业务涉及原订单取消、余座回滚、新订单创建、余座扣减、差价计算、状态更新等多个步骤，任何一步失败都会导致数据不一致，同时需处理差价的待支付金额与退款逻辑。
- **解决方案**：
    
    1. 全程包裹在单一数据库事务中，任何步骤失败立即全量回滚，保障操作原子性。
    2. 拆解核心步骤：计算原订单已支付金额 → 取消原订单（复用当前事务，不单独提交）→ 创建新订单（复用当前事务，不单独提交）→ 更新原订单状态为已改签 → 计算新旧订单差价 → 更新新订单待支付金额 → 同步新订单状态。
    3. 差价处理逻辑：新订单价格高于已支付金额时，差额计入待支付；低于已支付金额时，待支付金额清零，符合业务规则。
    4. 每一步操作都校验执行结果，影响行数为 0 时立即返回错误并触发回滚，避免无效操作继续执行。
    
- **效果**：实现改签业务全流程原子性操作，彻底解决多步骤操作的数据一致性问题，经过多轮异常场景测试，零数据不一致故障，保障了订单业务的可靠性。

## 四、项目成果与个人收获

1. 项目完成全业务流程闭环，覆盖 100% 核心功能与边界异常场景，通过功能、并发与稳定性测试，支持多客户端同时访问，服务端稳定运行无崩溃，顺利完成项目验收。
2. 封装的 DBManager 数据访问层被全项目复用，代码复用率提升 80%，大幅降低了业务层开发成本，同时保障了数据库操作的规范性与安全性。
3. 深入理解了 C/S 架构设计思想、数据库事务原理、TCP 网络编程等核心技术，掌握了 Qt 框架下桌面端与服务端的完整开发流程。
4. 提升了 C++ 模块化设计能力、复杂业务逻辑拆解能力、问题排查与调试能力，以及团队协作中的跨模块联调配合能力。






# 初版
我(陈业伟)要编写我的项目经历，我主要负责：
1.数据库表的设计+数据库操作的封装(单例模式，连接，关闭连接，事务操作，对不同的表增删改查的封装)
#ifndef DBMANAGER_H
#define DBMANAGER_H

#include <QSqlDatabase>
#include <QSqlQuery>
#include <QSqlError>
#include <QString>
#include <QList>
#include <QPair>
#include <QVariant>     //类型转换
#include <QDateTime>    //时间类型
#include "Common/Models.h"  //引入数据类型

//操作结果的状态
enum class DBResult
{
    Success,
    ConnectFailed,
    QueryFailed,
    NoData,
    TransactionFailed,
    updateFailed
};

class DBManager
{
public:
    /*
     * 单例模式
     * 避免资源浪费
     * 保证连接状态一致
     * 简化调用逻辑(统一使用instance获取该对象)
    */
    static DBManager& instance();  //获取数据库连接对象
    DBManager(const DBManager&)=delete;
    DBManager& operator=(const DBManager&)=delete;

    //errMsg作为传出参数：给 调用者/用户 提示信息
    //连接数据库
    bool connect(const QString& host,int port,const QString& user,const QString& passwd,const QString& dbName,QString* errMsg=nullptr);

    bool isConnected() const;

    //查询操作
    QSqlQuery Query(const QString& sql,const QList<QVariant>& params = QList<QVariant>(),QString* errMsg=nullptr);

    //增删改操作  返回受影响的行数
    int update(const QString& sql,const QList<QVariant>& params = QList<QVariant>(),QString* errMsg=nullptr);

    //事务操作
    bool beginTransaction();
    bool commitTransaction();
    bool rollbackTransaction();

    //封装与业务相关的数据库操作
    //用户                    //user作为传出参数
    DBResult getUserByUsername(const QString& username,Common::UserInfo& user,QString* errMsg=nullptr);
    DBResult getUserById(qint64 userId,Common::UserInfo& user,QString* errMsg=nullptr);
    DBResult getUserByPhone(const QString& phone, Common::UserInfo& existUser, QString* errMsg=nullptr);
    DBResult getUserByIdCard(const QString& id_card, Common::UserInfo& existUser, QString* errMsg=nullptr);
    DBResult addUser(const QString& username,const QString& password,const QString& phone,const QString& real_name,const QString& id_card,QString* errMsg=nullptr);
    DBResult updatePasswdByUsername(const QString& username,const QString& newPasswd,QString* errMsg=nullptr);
    DBResult updatePhoneByUsername(const QString& username,const QString& phone,QString* errMsg=nullptr);
    //常用乘机人
    DBResult addPassenger(const qint64 user_id,const QString& passenger_name,const QString& passenger_id_card,QString* errMsg=nullptr);
    DBResult delPassenger(const qint64 user_id,const QString& passenger_name,const QString& passenger_id_card,QString* errMsg=nullptr);
    DBResult getPassengers(const qint64 user_id,QList<Common::PassengerInfo>& passengers,QString* errMsg=nullptr);
    DBResult getPassenger(const qint64 user_id,const QString& passenger_name,const QString& passenger_id_card,Common::PassengerInfo& existPassenger,QString* errMsg=nullptr);
    //航班                    //flights作为传出参数
    DBResult searchFlights(const Common::FlightQueryCondition& cond,QList<Common::FlightInfo>& flights, QString* errMsg=nullptr);

    //城市列表
    DBResult getCityList(QList<QString>& fromCities,QList<QString>& toCities,QString* errMsg=nullptr);

    //订单                    //order作为传出参数
    DBResult createOrder(Common::OrderInfo& order,bool autoManageTransaction=true,QString* errMsg=nullptr);
    DBResult getOrderByFlightId(const qint64 flightId,const QString& passengerName,const QString& passengerIdCard,Common::OrderInfo& existOrder,QString* errMsg=nullptr);
    DBResult payForOrder(qint64 orderId,QString* errMsg=nullptr);     //修改订单状态->已支付
    DBResult getOrdersByUserId(qint64 userId,QList<QPair<Common::OrderInfo,Common::FlightInfo>>& ordersAndflights,QString* errMsg=nullptr);
    DBResult getOrdersByRealName(const QString& realName,const QString& idCard,QList<QPair<Common::OrderInfo,Common::FlightInfo>>& ordersAndflights,QString* errMsg=nullptr);     //本人订单
    DBResult rescheduleOrder(Common::OrderInfo& oriOrder,Common::OrderInfo& newOrder,qint32& priceDif,QString* errMsg=nullptr);
    DBResult cancelOrder(qint64 orderId,bool autoManageTransaction=true,QString* errMsg=nullptr);

private:
    DBManager();       //单例模式
    ~DBManager();

    //数据库连接对象
    QSqlDatabase db;

    //将查询结果转换为相应的Info
    Common::UserInfo userFromQuery(const QSqlQuery& query,const QString prefix="");
    Common::FlightInfo flightFromQuery(const QSqlQuery& query,const QString prefix="");
    Common::OrderInfo orderFromQuery(const QSqlQuery& query,const QString prefix="");
    Common::PassengerInfo passengerFromQuery(const QSqlQuery& query,const QString prefix="");
};
2.clienthandler的操作：接收客户端发来的JSON消息，根据JSON解析得出type进行相应的操作(登陆、修改密码、查询航班、查询订单等操作)，然后通过查询数据库，返回相应的JSON数据给客户端
3.在线用户管理：QMap<ClientHandler*, Common::UserInfo> m_users;  //连接->用户信息映射

难点：
1.运行用户自定义条件查询
DBResult DBManager::searchFlights(const Common::FlightQueryCondition& cond,QList<Common::FlightInfo>& flights, QString* errMsg)
{
    QString sql="select * from flight";
    QList<QVariant> params;
    QStringList whereClauses;   //存储查询条件(where)

    //动态拼接查询条件
    if(cond.id!=0)
    {
        whereClauses.append("id=?");
        params.append(cond.id);
    }
    if(!cond.fromCity.isEmpty())
    {
        whereClauses.append("from_city=?");
        params.append(cond.fromCity);
    }
    if(!cond.toCity.isEmpty())
    {
        whereClauses.append("to_city=?");
        params.append(cond.toCity);
    }
    if(cond.minDepartDate.isValid())
    {
        whereClauses.append("date(depart_time)>=?");
        params.append(cond.minDepartDate.toString("yyyy-MM-dd"));
    }
    if(cond.maxDepartDate.isValid())
    {
        whereClauses.append("date(depart_time)<=?");
        params.append(cond.maxDepartDate.toString("yyyy-MM-dd"));
    }
    if(cond.minDepartTime.isValid())
    {
        whereClauses.append("time(depart_time)>=?");
        params.append(cond.minDepartTime.toString("HH:mm"));
    }
    if(cond.maxDepartTime.isValid())
    {
        whereClauses.append("time(depart_time)<=?");
        params.append(cond.maxDepartTime.toString("HH:mm"));
    }
    if(cond.minPriceCents>0)
    {
        whereClauses.append("price_cents>=?");
        params.append(cond.minPriceCents);
    }
    if(cond.maxPriceCents>0 && cond.maxPriceCents>=cond.minPriceCents)
    {
        whereClauses.append("price_cents<=?");
        params.append(cond.maxPriceCents);
    }
    whereClauses.append("seat_left>0");

    //拼接完整SQL
    if(!whereClauses.isEmpty())  sql+=" where " + whereClauses.join(" and ");

    //添加：按起飞时间升序排列
    sql+=" order by depart_time asc ";

    qInfo() << "(查询航班)拼接后的SQL：" << sql;
    qInfo() << "查询参数：" << params;

    //执行sql
    QSqlQuery query=Query(sql,params,errMsg);
    if(!query.isActive())
    {
        if(errMsg) *errMsg=*errMsg+" 航班查询失败";
        return DBResult::QueryFailed;
    }

    //解析结果
    flights.clear();
    flights.reserve(query.size());

    while(query.next())
    {
        flights.append(flightFromQuery(query));
    }

    return flights.isEmpty()?DBResult::NoData : DBResult::Success;
}
2.改签：处理的逻辑比较绕：先计算已支付金额 再取消订单 再创建新订单 再更新原订单状态 计算差价 更新新订单待支付金额/退款 
//改签
DBResult DBManager::rescheduleOrder(Common::OrderInfo& oriOrder,Common::OrderInfo& newOrder,qint32& priceDif,QString* errMsg)
{
    //开启事务
    if(!beginTransaction())
    {
        if(errMsg) *errMsg="开启事务失败";
        return DBResult::TransactionFailed;
    }

    //已支付金额=原价-待支付金额
    qint32 oriPaidAmount=oriOrder.priceCents-oriOrder.pendingPayment;

    //取消订单
    DBResult res=this->cancelOrder(oriOrder.id,false,errMsg);
    if(res != DBResult::Success)
    {
        if(errMsg) *errMsg=*errMsg+" 取消订单失败";
        rollbackTransaction();
        return res;
    }

    //创建订单
    res=this->createOrder(newOrder,false,errMsg);
    if(res != DBResult::Success)
    {
        if(errMsg) *errMsg=*errMsg+" 创建新订单失败";
        rollbackTransaction();
        return res;
    }

    //更新原订单状态为Rescheduled
    oriOrder.status = Common::OrderStatus::Rescheduled;
    QString updateStatusSql1 = "update orders set status=? where id=?";
    QList<QVariant> statusParams1;
    statusParams1 << static_cast<int>(oriOrder.status) << oriOrder.id;
    int statusAffected1 = update(updateStatusSql1, statusParams1, errMsg);
    if(statusAffected1 <= 0)
    {
        if(errMsg) *errMsg=*errMsg+" 新订单状态更新失败";
    }

    //差价=新订单价格(创建订单后才有实际价格)-已支付价格
    priceDif=newOrder.priceCents-oriPaidAmount;
    //新订单待支付金额=max(0,差价)  避免被createOrder内部初始化覆盖
    newOrder.pendingPayment=qMax(0,priceDif);

    //更新新订单的pendingPayment(因为createOrder覆盖了 此处需同步到数据库)
    QString updatePendingSql = "update orders set pending_payment=? where id=?";
    QList<QVariant> updateParams;
    updateParams<<newOrder.pendingPayment<<newOrder.id;
    int updateAffected = update(updatePendingSql, updateParams, errMsg);
    if(updateAffected<=0)
    {
        if(errMsg) *errMsg=*errMsg+" 新订单待支付金额更新失败";
    }

    //根据pendingPayment->更新新订单状态(大于0->Booked 等于0->Paid)
    qDebug()<<"newOrder.pendingPayment:"<<newOrder.pendingPayment;
    newOrder.status = newOrder.pendingPayment>0 ? (Common::OrderStatus::Booked) : (Common::OrderStatus::Paid);
    QString updateStatusSql2 = "update orders set status=? where id=?";
    QList<QVariant> statusParams2;
    statusParams2 << static_cast<int>(newOrder.status) << newOrder.id;
    int statusAffected2 = update(updateStatusSql2, statusParams2, errMsg);
    if(statusAffected2 <= 0)
    {
        if(errMsg) *errMsg=*errMsg+" 新订单状态更新失败";
    }


    //提交事务
    if(!commitTransaction())
    {
        rollbackTransaction();
        if(errMsg) *errMsg="提交事务失败";
        return DBResult::TransactionFailed;
    }

    return DBResult::Success;
}






















